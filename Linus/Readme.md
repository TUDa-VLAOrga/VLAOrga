This project was based on an outdated version of Linus.
A test instance of this version could be found here before commit 9cca55b.
The corresponding infrastructure behind starting the app was in that commit as well.
If you want to set up a test instance of the current version of Linus, revert to the previous Docker infrastructure.
and adapt the according Dockerfiles and the submodule to suit the latest versions' needs. 
