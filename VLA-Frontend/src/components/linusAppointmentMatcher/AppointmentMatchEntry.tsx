import "@/styles/AppointmentMatching.css"

interface AppointmentMatchEntryProps {
    linusAppointmentId: number
}

export default function AppointmentMatchEntry({linusAppointmentId} : AppointmentMatchEntryProps) {
    return ""
}